# AI Native 前端重构规格

## 目标

在不改变 V4 后端策略与接口契约的前提下，将量化助手重构为面向盘中判断的单任务工作台。默认界面优先回答：

1. 当前标的发生了什么；
2. 当前处于观察、增强还是确认阶段；
3. 系统依据是什么；
4. 用户下一步应继续观察什么。

## 信息架构

### 顶部 Command Header

- 产品标识：QUANT COPILOT / 分时顶底助手
- 全局连接状态、系统时钟、Windows 提醒状态
- 启动或暂停监控
- 打开“控制中心”

### 今日关注 Rail

- 横向展示 WindCode、最新价和涨跌
- 点击切换主标的
- 添加与删除标的收进控制中心

### 决策主舞台

- 左侧：当前标的大价格、行情时间、30 秒价格图、VWAP 与真实事件标记
- 右侧：确定性的“系统现在看到什么”简报
- 状态路径：观察结构 → 证据增强 → 确认提醒
- 候选必须明确为观察信息；只有 CONFIRMED 才显示买入或卖出提醒

### 下方工作区

- 事件时间线：候选、增强、确认、失效的最新事件
- 今日状态：候选数、确认数、通道、预热、反转进度、后端状态与错误

### 控制中心抽屉

- 今日关注列表维护
- 灵敏、标准、稳健预设
- 全部基础与高级 V4 参数
- 候选提醒与 Windows 提醒设置
- 参数保存、提醒测试、运行诊断

## 视觉规范

- 背景：深黑石墨
- 主色：单一紫蓝
- 语义色：实时或买入为 mint，候选为 amber，卖出为 coral，失效为 slate
- 主价格使用超大字号和 tabular numbers
- 20 至 28 像素圆角，极细描边，大量留白
- 光晕和网格纹理只用于建立空间层次，不干扰数据
- 宽屏以主图为视觉中心；移动端改为单列，抽屉全宽

## 安全与真实性

- 不添加模拟行情、示例信号或 AI 预测
- 不改变 API 路径、请求体、刷新机制或 V4/legacy 字段
- 后端离线时只显示诚实的就绪态
- 固定声明：候选 ≠ 交易指令；系统只提醒，不自动交易。

## 前端验收

- npm run lint
- npm test
- 宽屏、1024 像素与 760 像素以下响应式检查
- 控制中心打开、关闭、遮罩和 Esc 检查
- 无横向溢出，焦点态可见，减少动态效果设置生效
