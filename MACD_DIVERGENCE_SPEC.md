# 自适应多尺度动能策略 V2.0

## 定位

这是“分时波段”之外的独立提醒通道，前端名称为“多尺度动能”，内部版本为 MDC-MACDV-2.0。它解决的不是“是否出现一个很小的 MACD 背离”，而是三个连续问题：

1. 当前转折属于微级、小级、中级还是大级？
2. MACD-V 处于动能扩张、衰减还是反向阶段？
3. 该事件应忽略、观察、列为候选，还是形成正式买卖建议？

它可以独立运行，也可以与分时波段策略同时运行。所有输出都只是人工决策提醒，不执行自动交易。

本实现是对 Directional Change、Multi-threshold Directional Change 与 MACD-V 文献的工程化组合，不是对某一篇论文交易规则的逐字复刻，也不代表已经获得 A 股实盘有效性证明。

## 数据与因果约束

- 输入为已完成的 60 秒 OHLCV 决策 K；30 秒 K 只用于图表。
- 每根完整分钟 K 只处理一次；盘前、午间与收盘后数据不参与。
- 所有转折必须等待价格从极值产生足够反向变化后才能确认，不使用未来 K 线，不移动已经记录的极值。
- EMA、ATR、Directional Change 状态与事件去重均按交易日重置。
- 快速启动模式至少积累 15 根完整决策分钟 K；仍使用标准 EMA(12/26/9) 与 ATR(26) 递推，ATR 在不足 26 根时使用已有样本初始化，不再额外等待第二个 26 根门槛。同日重启优先回放缓存恢复状态，不补发历史 Toast。

## MACD-V 动能

基础参数仍为 12 / 26 / 9，但不再用“DIF ÷ 股价”比较不同股票，而使用 ATR 做波动归一：

    MACD-V = (EMA(12) - EMA(26)) / ATR(26) × 100

    Signal = EMA(9, MACD-V)

    Histogram = MACD-V - Signal

动能阶段采用以下区间：

| MACD-V 区间 | 与 Signal 的关系 | 阶段 |
| --- | --- | --- |
| > 150 | 任意 | 风险过热 |
| 50 ~ 150 | 在 Signal 上方 / 下方 | 上行动能扩张 / 上行动能回落 |
| -50 ~ 50 | 任意 | 动能中性 |
| -150 ~ -50 | 在 Signal 上方 / 下方 | 下行动能修复 / 下行动能扩张 |
| < -150 | 任意 | 风险超跌 |

卖出方向要求 MACD-V 斜率与柱体共同转弱；买入方向镜像。若卖出确认时动能已经进入明显负区，或买入确认时已经进入明显正区，事件会降级，避免卖在短线低位、买在短线高位。

## MDC 自动定级

系统并行维护四个 Directional Change 阈值。阈值取“固定下限”和“当前 ATR% × 倍数”中的较大值，并在一段方向波动开始时冻结，避免行情变化后回改历史级别。

| 级别 | ATR% 倍数 | 最小反向变化 | 前端用途 |
| --- | ---: | ---: | --- |
| 微级 | 0.75 | 0.10% | 同级背离 + 动能转折时形成早期提醒 |
| 小级 | 1.20 | 0.18% | 满足确认门控时形成正式建议 |
| 中级 | 1.80 | 0.32% | 可形成正式建议 |
| 大级 | 2.60 | 0.55% | 可形成正式建议 |

高点形成后，收盘价从该高点回落达到本级冻结阈值，判定本级顶部 Directional Change；低点后的反弹规则镜像。系统还要求：

- 中级波段至少运行 4 根 K，波段幅度至少为本级阈值的 1.25 倍；
- 大级波段至少运行 6 根 K，波段幅度至少为本级阈值的 1.40 倍。

因此，“级别”同时由当前波动率、反向幅度、原波段幅度和持续时间共同约束，不是用户手填一个固定差价。

## 多尺度一致与高级别门控

四个尺度权重分别为 1、2、3、4。顶部卖出方向统计已经转为下行的尺度，底部买入方向统计已经转为上行的尺度，得到 consensus_pct。

- 小级反向信号若与中级、大级趋势同时冲突，只保留内部观察。
- 小级以上、结构完整、MACD-V 同向转折且多尺度一致度达到 55%，才允许形成正式确认；微级不等待高级别同步，只在同级背离与动能转折同时成立时形成 EARLY 早期提醒。
- 同一个物理顶底从中级继续升级为大级时，前端更新同一事件 revision，不重复发送正式 Toast。
- 同级常规 MACD-V 顶/底背离会增加证据和置信度，但不再是必要条件，也不能单独触发正式建议。

## 输出层级

| 引擎层级 | 前端状态 | Windows 提醒 |
| --- | --- | --- |
| WATCH | 观察 | 否 |
| CANDIDATE | 候选 / 增强 | 否 |
| CONFIRMED | 确认，显示买入建议或卖出建议 | 是 |

微级 EARLY 提醒同样进入前端事件流并发送 Windows 提醒，但文案明确标注“早期提醒”，表示高级别趋势尚未同步；小级、中级和大级仍按正式确认门控执行。

## 缓存、开关与迁移

- “分时波段”和“多尺度动能”各有独立开关，可单独或同时运行。
- 策略关闭时仍持续维护指标上下文，重新开启不需要重新预热，也不补发关闭期间事件。
- 旧版 MACD-DIV-1.0 缓存事件不会混入新版事件流；分钟 K 会照常回放来恢复新版状态。
- 跨交易日只保留关注列表和设置，不比较昨日与今日极值。

## 验证边界

当前默认阈值已用项目内当日真实缓存做过提醒密度回放，但这只是工程校准，不是收益回测。正式使用前仍需按标的统计：

- 每个级别的事件数与正式提醒数；
- 极值到提醒的延迟和价格损失；
- 提醒后最大有利 / 不利波动；
- 手续费、滑点后的可操作空间；
- 不同波动环境和不同股票上的稳定性。

参考文献：

- [Multi-threshold Directional Change（多阈值视角与加权建议）](https://repository.essex.ac.uk/28511/1/DC-GA.pdf)
- [Directional-change intrinsic-time profiling](https://ideas.repec.org/a/taf/quantf/v17y2017i2p217-225.html)
- [Improved Directional Change with regime detection](https://arxiv.org/abs/2309.15383)
- [MACD-V: Volatility Normalised Momentum](https://cmtassociation.org/wp-content/uploads/2022/05/MACD-V-Volatility-Normalised-Momentum-by-Alex-Spiroglou-DipTA-ATAA-CFTe.pdf)
