# 自适应多因子盘中策略 V2

## 目标

把当前“距离近期高低点达到固定百分比即触发”的规则，替换成一套适合 A 股盘中做 T 提醒的、可解释且不依赖持仓的多因子信号系统。系统只提醒，不下单，不声称收益保证。

## 数据层

- 行情继续使用 WindPy `wsq`，轮询间隔约 1 秒。
- 用 `rt_time + rt_last + rt_vol + rt_amt` 去除完全重复的报价，避免静止报价被当成新样本。
- 按 30 秒聚合微型 K 线，字段至少包括：`open/high/low/close/vwap/volume/amount/timestamp/time`。
- `rt_vol`、`rt_amt` 是累计值，微型 K 线量额必须使用相邻累计值的非负增量。
- 每个代码保存不超过当日需要的滚动窗口；前端图表使用聚合 K 线，不再每秒追加重复价格点。
- 09:30 前、午间休市、15:00 后不产生信号；默认 09:35 前只积累数据。
- 数据不足、报价长期不更新、价格或量额异常时不产生信号，并把原因暴露给前端。

## 指标

基于已完成 K 线加当前 K 线计算：

- EMA(5)、EMA(13)
- ATR(14)
- RSI(7)，使用 Wilder 口径或等价稳定实现
- 20 根窗口效率比 ER：`abs(C_t-C_0) / sum(abs(delta C))`
- VWAP 距离，以 ATR 标准化：`(price-vwap)/ATR`
- VWAP 短期斜率，以 ATR 标准化
- 当前 K 线增量成交量 / 过去 10 根完整 K 线成交量中位数
- 20 根区间位置与前高/前低

所有除法必须处理 0、空值和样本不足。

## 市场状态

- `TREND_UP`：EMA5 > EMA13、价格在 VWAP 上方、ER 和斜率达到趋势阈值。
- `TREND_DOWN`：上述条件镜像。
- `RANGE`：ER 较低且 EMA 间距相对 ATR 较小。
- `NEUTRAL`：其余情况。

状态仅用于选择信号模块，不直接产生买卖提醒。

## 四个信号模块

1. 趋势回踩/反抽
   - 上升趋势中，价格回踩 EMA5/VWAP 后重新站回，产生 BUY 候选。
   - 下降趋势中，价格反抽 EMA5/VWAP 后重新跌回，产生 SELL 候选。

2. 区间均值回归
   - RANGE 中，价格低于 VWAP 达到 ATR 扩展阈值、RSI 偏低且出现向上反转，产生 BUY 候选。
   - RANGE 中，价格高于 VWAP 达到 ATR 扩展阈值、RSI 偏高且出现向下反转，产生 SELL 候选。

3. 放量突破/跌破
   - 突破过去窗口前高并超过 ATR 缓冲、成交量比达到阈值，产生 BUY 候选。
   - 跌破过去窗口前低并超过 ATR 缓冲、成交量比达到阈值，产生 SELL 候选。

4. 冲高/杀跌衰竭（做 T 提醒）
   - 价格高于 VWAP 多个 ATR、RSI 偏高、短期动量减弱并转跌，产生 SELL 候选。
   - 价格低于 VWAP 多个 ATR、RSI 偏低、短期动量减弱并转涨，产生 BUY 候选。

## 评分与门禁

- 每个候选按环境、价格结构、VWAP、量能、波动率和反转确认累计 0–100 分。
- 至少三个相互独立的确认项，且达到 `min_confidence` 才能触发。
- 默认 `min_confidence = 72`、`cooldown_seconds = 180`、`warmup_bars = 12`、`opening_guard_minutes = 5`、`volume_ratio_threshold = 1.15`、`atr_extension = 1.2`。
- 同一代码同方向不得连续触发；必须出现反方向信号或状态重置后才允许再次触发。
- 同一 K 线最多一个信号。若多个模块同时成立，保留得分最高者。

## API 与解释性

保留现有 API 路径和 Windows 提醒能力。信号增加：

- `module` / `module_label`
- `regime` / `regime_label`
- `confidence`（0–100）
- `confirmations`（中文数组）
- `metrics`（EMA、ATR、RSI、VWAP 距离、量比、ER 等）
- `rationale`（一句中文结论）

状态增加每个代码的 `analytics`：是否 ready、预热进度、市场状态、关键指标、被阻断原因和最近更新时间。

## 前端

- 删除买入/卖出单一触发器下拉框和固定百分比描述。
- 今日规则改为“自适应多因子盘中策略 V2”。
- 可配置项：最低置信分、冷却秒数、预热 K 线数、开盘保护分钟、量比阈值、ATR 扩展倍数、提醒开关。
- 当前标的展示：预热进度/策略就绪、市场状态、EMA 方向、VWAP 距离、ATR、RSI、量比。
- 信号卡展示：BUY/SELL、模块、市场状态、置信分、确认项、价格和时间。
- 空状态应明确说明“正在预热/没有达到多因子门槛”，不能出现示例或虚构信号。
- 保留现有暗色视觉方向、WindCode 管理、真实行情图、Windows 提醒测试和 hydration 安全时钟。

## 兼容与验证

- 后端必须兼容 Python 2.7，不引入第三方 Python 包。
- 保留中文路径下 Windows 通知路径的 `mbcs` 修复。
- 保留本机绑定 `127.0.0.1:8765`，不得修改系统代理、路由或防火墙。
- 对旧配置做向后兼容归一化，但前端只提交 V2 配置。
- 增加确定性单元测试，至少覆盖：预热不触发、趋势回踩、区间回归、放量突破、衰竭、门槛不足、冷却/方向去重、重复报价去重。
- 最终必须通过 Python 2.7 编译、后端测试、`npm test`、`npm run lint` 和 `git diff --check`。
